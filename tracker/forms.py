# from django.utils import timezone
#
# from .models import Tracking
#
# class TrackCreateForm(forms.Form):
#     railcar = forms.ModelChoiceField(Tracking.railcar)
#     amount = forms.DecimalField(max_value=10000, min_value=0, decimal_places=2)
#     comment = forms.Textarea
#     datetime = forms.DateTimeField(initial=timezone.now())
